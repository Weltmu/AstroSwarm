# 联网搜索能力包（web-search）

让 AI 拥有联网搜索能力：遇到知识库没有的实时、外部、对比类问题，
自动搜索 Bing 并返回带来源链接的结果。

## 使用方式

安装本能力包后，AI 会在需要时自动调用 `web_search` 工具（无需手动触发）。

## 说明

- 走 Bing RSS 免费接口，无需额外 API 密钥；
- 每轮对话由模型自行决定搜索次数（建议在系统提示里限制，如最多 2 次）；
- 搜索失败返回可读错误说明，AI 会如实告知，不会硬编。
