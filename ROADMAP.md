# 路线图 Roadmap

本文件记录 AstroSwarm 星群接下来的开发方向。条目会随进度更新，欢迎在 Issues 里讨论优先级。

## 一、AI 插件工坊（规划中，优先级最高）

**一句话**：在软件里内置一个工作台 —— 用户用自己的话描述需求，AI 把它变成能用的插件，校验通过后直接装进机器人。

### 用户视角的流程

1. 打开「插件工坊」，用大白话写需求，例如：
   - 「每天早上 8 点把当天天气发到群里」
   - 「群里有人说『签到』就记一次，满 7 天给个头衔」
   - 「把 B 站链接转成摘要卡片发出来」
2. AI 先回一份**实现计划**：需要几个工具函数、要哪些参数、是否联网、会不会读写群成员数据。
3. 点「生成」→ AI 写出插件目录：`manifest.json` + `tools/*.py`（+ 可选 `README.md`）。
4. 程序做**静态检查与干跑**：解析语法、校验 manifest、用假消息调用一遍工具（限制超时与网络，不碰真实群）。
5. 全绿后一键装到 `assets/`，控制台里启用；不满意可以「再改一版」，每版留历史、可回滚。

### 为什么做

- 插件规范本身很轻（一个目录 + `manifest.json` + `tools/*.py`），非常适合让模型直接生成。
- 用户需求千差万别，靠官方人工更新插件永远追不上。
- 形态参考 Codex / DeepSeek Harness 这类「对话 → 计划 → 改代码 → 验证」的工作台。

### 技术要点（初步）

| 模块 | 做法 |
| --- | --- |
| 需求理解 | 复用「AI 大脑」里已配置的模型（DeepSeek / 智谱 / OpenAI / 自定义），不额外要求用户再配 key |
| 代码生成 | 先生成到临时目录，不直接写运行中的插件目录 |
| 校验 | `py_compile` / `ast` 语法检查 + manifest 校验 + 干跑（假数据、限超时、限网络） |
| 权限 | 插件必须声明所需能力（网络 / 文件 / 群操作），未声明就拒绝；默认不放开 shell 与任意文件写入 |
| 人工确认 | 涉及发消息、群管理、读写用户数据的插件，安装前必须人工点一次确认 |
| 版本与回滚 | 每次生成落一个版本目录，一键回滚上一版 |
| 分发 | 导出 zip 分享，或走插件市场清单（`assets/plugin_market/`） |

### 里程碑

- **M1**：工坊界面 + 「需求 → 插件骨架」，可生成、可安装
- **M2**：工具代码生成 + 静态检查 + 干跑，全绿才允许安装
- **M3**：试运行与调试回路（把运行报错回灌给模型自动修）
- **M4**：插件能力声明与沙箱（网络 / 文件 / 群操作白名单）
- **M5**：一键分享与市场分发

## 二、其它方向

- **插件权限模型**：把「这个插件能做什么」显式声明出来，控制台可见、可关
- **多通道能力对齐**：QQ / 微信 / 飞书 / Telegram 的语音、文件、引用回复等能力对齐
- **记忆资产**：记忆可导出、时间线可回看，便于迁移与训练
- **部署安全**：无头端正式部署默认只监听 127.0.0.1、内置登录鉴权与 HTTPS 指引

## 三、近期已完成

- v1.2.4：安装包内直接完成机器人运行环境部署，装完即用
- v1.2.5：安装目录与机器人目录整合（默认 `安装目录\机器人`），卸载一并清理
- 启动自清理：`%TEMP%` 下的解压残留、中断安装的临时目录、过期缓存
